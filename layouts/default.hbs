<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    {{#description}}
    <meta name="description" content="{{.}}">
    {{/description}}

    <title>{{title}}</title>

    {{> styles}}
  </head>
  <body>
    {{> main_menu}}

    <div class="board">
      <div class="content">
        {{{contents}}}
      </div>
    </div>

    {{> scripts}}
  </body>
</html>
